[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .

# Phase 0：核心 RAG 链路 service 覆盖率门槛（分块 / 向量 / LLM / 检索）
addopts = --cov=app.services.chunking_service --cov=app.services.embedding_service --cov=app.services.llm_service --cov=app.services.rag_service --cov-report=term-missing --cov-fail-under=60
